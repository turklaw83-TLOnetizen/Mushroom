# Project Mushroom Cloud — Legal Intelligence Suite Dependencies
#
# API Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
alembic>=1.14.0

# Auth
clerk-backend-api>=1.0.0
PyJWT>=2.9.0

# API Testing
httpx>=0.27.0

# ---- Existing Dependencies ----
# Core framework
streamlit>=1.40.0
streamlit-autorefresh>=1.0.1
pyyaml>=6.0

# LLM providers
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-xai>=0.2.0
langchain-google-genai>=2.0.0
langchain-community>=0.3.0
langgraph>=0.2.0

# Embeddings & vector store
langchain-openai>=0.2.0
langchain-huggingface>=0.1.0
chromadb>=0.5.0

# Document processing
pypdf>=4.0.0
python-docx>=1.0.0
fpdf2>=2.8.0
openpyxl>=3.1.0
Pillow>=10.0.0

# OCR (optional)
pytesseract>=0.3.10

# Token counting
tiktoken>=0.7.0

# Data validation
pydantic>=2.0.0

# Web search
duckduckgo-search>=6.0.0

# Authentication
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0

# Calendar sync (optional)
google-api-python-client>=2.0.0

# Email (billing)
# Uses stdlib smtplib - no extra dependency

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Desktop wrapper
pywebview>=5.0.0

# Error tracking
sentry-sdk[fastapi]>=2.0.0

# Cloud backup
b2sdk>=2.0.0

# Anthropic direct (non-LangChain calls)
anthropic>=0.28.0

# Utilities
python-dotenv>=1.0.0

# ---- Phase 6-24 New Dependencies ----

# WebSocket support (Phase 12)
websockets>=12.0

# Push notifications (Phase 14)
pywebpush>=2.0.0
py-vapid>=1.9.0

# Email integration (Phase 15)
imapclient>=3.0.0
exchangelib>=5.0.0

# Vector search / RAG (Phase 16)
sentence-transformers>=3.0.0
numpy>=1.26.0
rank-bm25>=0.2.2

# Calendar (Phase 17)
icalendar>=5.0.0

# Transcription (Phase 18)
# pyannote.audio — optional, requires torch; install separately if GPU available

# Observability (Phase 19+20)
prometheus-client>=0.21.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-exporter-otlp>=1.25.0

# Multi-tenancy (Phase 22)
msal>=1.28.0

# File security (Phase 24)
pyclamd>=0.4.0
reportlab>=4.0.0
PyPDF2>=3.0.0
cryptography>=42.0.0

# Async testing
pytest-asyncio>=0.23.0
